#!/bin/bash

# Launch the Misty bluetooth controls TUI (provided by bluetui).
# Also attempts to unblock bluetooth service if rfkill had blocked it.

rfkill unblock bluetooth
exec misty-launchfocus-tui bluetui
