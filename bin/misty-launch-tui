#!/bin/bash

# Launch the TUI command passed in as an argument in the default terminal with an org.misty.COMMAND app id for styling.

exec setsid alacritty --app-id=org.misty.$(basename $1) -e "$1" "${@:2}"
