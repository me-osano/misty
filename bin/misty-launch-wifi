#!/bin/bash

# Launch the Misty wifi controls (provided by the NetworkManager TUI).
# Attempts to unblock the wifi service first in case it should be been blocked.

rfkill unblock wifi
exec $TERMINAL --class=org.misty.terminal -e bash -c 'nmtui; exec bash'
